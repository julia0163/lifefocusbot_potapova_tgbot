services:
  - type: web
    name: telegram-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python tgbot.py
    envVars:
      - key: TOKEN
        sync: false
    autoDeploy: true
